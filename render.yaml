services:
  - type: web
    name: project-kalam
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn simple_app:app
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
